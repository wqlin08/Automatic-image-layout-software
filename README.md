# [YourSoftwareName] - Scientific Image Composition Tool
# [YourSoftwareName] - 科研论文组图自动排版神器

> **Stop aligning figures manually in PowerPoint!**  
> **彻底告别在 PPT 中手动对齐图片的痛苦时光！**

![Software Screenshot](screenshot.png)
*(Screenshot: Automatic layout with smart label synchronization)*

## 📖 Introduction (简介)

**[YourSoftwareName]** is a lightweight Windows tool designed for researchers and students. It automates the process of creating multi-image figures for academic papers. Unlike PowerPoint or Photoshop, it ensures perfect alignment, uniform spacing, and consistent labeling with just a few clicks.

**[YourSoftwareName]** 是一个专为科研人员和学生设计的轻量级 Windows 工具。它能自动完成学术论文中多图组合的排版工作。与 PowerPoint 或 Photoshop 不同，它只需几次点击，即可保证图片完美对齐、间距均匀以及标签风格统一。

---

## ✨ Key Features (核心功能)

### 1. 🚀 Automatic Layout (自动网格排版)
- **Grid System:** Automatically arranges images in a grid (Rows/Columns).
- **Custom Spacing:** Precisely control X/Y gaps and outer margins using sliders.
- **网格系统：** 自动将导入的图片按行列网格排列。
- **自定义间距：** 通过滑块精确控制横向/纵向间距以及外边距（留白）。

### 2. 🧠 Smart Label Synchronization (智能标签同步) **(Highlight!)**
- **One-Click Sync:** Drag label `(a)` to your desired position, click "Smart Sync" (智能同步), and labels `(b)`, `(c)`, `(d)`... will automatically move to the **exact same relative position** on their respective images.
- **No more manual alignment!**
- **一键同步：** 只需要将第一个图片的序号 `(a)` 拖动到满意的位置，点击“智能同步”，后续所有图片的序号 `(b)`, `(c)`... 会自动跳转到**完全一致的相对位置**。
- **从此不再需要用肉眼对齐序号！**

### 3. 🔤 Professional Annotation (专业标注设置)
- **Custom Fonts:** Supports standard academic fonts (Arial, Times New Roman, etc.).
- **Rich Styling:** Adjustable font size, bolding, and custom content (automatic a,b,c,d generation).
- **自定义字体：** 支持论文常用字体（如 Arial, Times New Roman）。
- **丰富样式：** 支持调节字号、加粗，以及自定义序号格式。

### 4. 🖼️ Image Handling (图片处理)
- **Multi-format Support:** JPG, PNG, TIFF, BMP.
- **Individual Scaling:** Select a specific image to scale it individually (useful for images with different aspect ratios).
- **Reordering:** easily move images up or down the list.
- **多格式支持：** 支持 JPG, PNG, TIFF, BMP 等常见格式。
- **单独缩放：** 选中特定图片可单独调整缩放比例（解决素材尺寸不一的问题）。
- **排序调整：** 列表式管理，轻松调整图片顺序。

### 5. 📤 High-Quality Export (高清导出)
- **Custom DPI:** Define your output DPI (e.g., 300, 600) to meet journal requirements.
- **Formats:** Export combined figures as PNG, JPG, or TIFF.
- **自定义 DPI：** 可设置 300 或 600 DPI，满足期刊投稿清晰度要求。
- **格式选择：** 导出为 PNG, JPG 或 TIFF 格式。

---

## 🛠️ How to Use (使用指南)

1.  **Import:** Click `Add Images` (添加图片) to load your source files.
2.  **Layout:** Choose `Grid` (网格) mode, set the number of columns (e.g., Fixed Columns: 2). Adjust `X-Dist` and `Y-Dist` for spacing.
3.  **Labeling:**
    *   Enable labeling in the settings.
    *   Drag the label `(a)` on the preview canvas to the perfect spot.
    *   Click **`Smart Sync` (智能同步)** button.
4.  **Export:** Set your desired DPI (e.g., 300) and click `Save Image`.

1.  **导入：** 点击 `添加图片` 导入素材。
2.  **布局：** 选择 `网格` 模式，设置列数（例如定列：2）。调整 XY 间距和留白。
3.  **标注：**
    *   在设置中开启标注。
    *   在右侧预览区，鼠标拖拽 `(a)` 到你想要的位置。
    *   点击 **`智能同步`** 按钮，其余序号自动对齐。
4.  **导出：** 设置 DPI（如 300），点击 `保存图片`。

---

## 📥 Download (下载)

Get the latest executable (`.exe`) from the **[Releases Page](../../releases)**. No installation required, just unzip and run.

请前往 **[Releases 页面](../../releases)** 下载最新的 `.exe` 文件。绿色软件，解压即用，无需安装。

---

## ☕ Support (支持)

If this tool saved your time on your paper, give me a **Star** ⭐!

如果这个工具帮你节省了写论文的时间，请给我点一个 **Star** ⭐！

---

## 📄 License

MIT License © 2023 [Your Name/GitHub Username]
